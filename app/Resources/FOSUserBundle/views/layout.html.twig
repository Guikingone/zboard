{% extends ('base.html.twig')%}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="text-center">
                    <!-- On affiche le message d'informations pour la validation du compte -->
                    {% for message in app.session.flashbag('info') %}
                        <div class="alert alert-info">{{ message }}</div>
                    {% endfor %}
                    {% block fos_user_content %}

                    {% endblock fos_user_content %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}