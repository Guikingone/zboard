{% extends "::layout_mentor.html.twig" %}

{% block body %}
  <div class="row">
    <div class="col-md-12">
      <div class="box">
        <div class="box-header with-border">
          <h3 class="box-title">Soutenances en attente de validation</h3>
        </div>
        <!-- /.box-header -->
        <div class="box-body">
          {% if soutenances %}
            <table class="table table-bordered">
              <tr>
                <th style="width: 10px">#</th>
                <th>Prénom & Nom</th>
                <th>Parcours</th>
                <th>Etat de la soutenance</th>
                <th>Actions</th>
              </tr>
              {% for soutenance in soutenances %}
                <tr>
                  <td>{{ soutenance.id }}</td>
                  <td>{{ soutenance.mentore.firstname }} {{ soutenance.mentore.lastname }}</td>
                  <td>{{ soutenance.projet.libelle }}</td>
                  <td><span class="badge bg-aqua">En attente</span></td>
                  <td>
                    <button type="button" class="btn btn-flat btn-primary">Ajouter des documents</button>
                    <button type="button" class="btn btn-flat btn-info">Voir le détail</button>
                    <button type="button" class="btn btn-flat btn-success">Valider le projet</button>
                  </td>
                </tr>
              {% endfor %}
            </table>
          {% else %}
            <div class="alert alert-info">Vous n'avez pas encore de mentoré d'assigné. Patience !</div>
          {% endif %}
        </div>
        <!-- /.box-body -->
      </div>
      <!-- /.box -->
    </div>
  </div>
{% endblock %}
