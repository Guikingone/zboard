{% extends ':BackBundle/Layout:admin.html.twig' %}

{% block title %} - Compétences{% endblock %}

{% block admin_body %}

    {% for message in app.session.flashbag.get('success') %}
        <div class="alert alert-success alert-dismissible">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <h4><i class="icon fa fa-check"></i>Information</h4>
            {{ message }}
        </div>
    {% endfor %}

    <div class="row">
        <div class="col-xs-12 col-md-offset-4 col-md-4">
            <div class="box box-primary">
                <div class="box-body box-profile">
                    <h3 class="profile-username text-center">Modifier une compétences</h3>
                    <br />
                    <hr />
                    <div class="row">
                        <div class="col-xs-12 text-center">
                            {{ form_start(competence) }}
                            <div class="form-group">
                                {{ form_errors(competence.libelle) }}
                                {{ form_label(competence.libelle, 'Intitulé :')}}
                                {{ form_widget(competence.libelle, {'attr': {'class': 'form-control'}}) }}
                            </div>
                            <div class="form-group">
                                {{ form_errors(competence.projet) }}
                                {{ form_label(competence.projet, 'Projet lié :')}}
                                {{ form_widget(competence.projet, {'attr': {'class': 'form-control'}}) }}
                            </div>
                        </div>
                        <br>
                        <div class="text-center">
                            <input class="btn btn-warning" type="submit" value="Enregistrer" />
                        </div>
                        {{ form_end(competence) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    </div>
{% endblock %}