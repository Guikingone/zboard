{% extends ':BackBundle/Layout:admin.html.twig' %}

{% block title %} - Cours{% endblock %}

{% block admin_body %}

    {% for message in app.session.flashbag.get('success') %}
        <div class="alert alert-success alert-dismissible">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <h4><i class="icon fa fa-check"></i>Information</h4>
            {{ message }}
        </div>
    {% endfor %}

    <div class="row">
        <div class="col-xs-12 col-md-offset-4 col-md-4">
            <div class="box box-primary">
                <div class="box-body box-profile">
                    <h3 class="profile-username text-center">Modifier un cours</h3>
                    <br />
                    <hr />
                    <div class="row">
                        <div class="col-xs-12 text-center">
                            {{ form_start(cours) }}
                            <div class="form-group">
                                {{ form_errors(cours.libelle) }}
                                {{ form_label(cours.libelle, 'Nom du cours :')}}
                                {{ form_widget(cours.libelle, {'attr': {'class': 'form-control'}}) }}
                            </div>
                            <div class="form-group">
                                {{ form_errors(cours.duree) }}
                                {{ form_label(cours.duree, 'Durée du cours :')}}
                                {{ form_widget(cours.duree, {'attr': {'class': 'form-control'}}) }}
                            </div>
                            <div class="form-group">
                                {{ form_errors(cours.parcours) }}
                                {{ form_label(cours.parcours, 'Parcours lié :')}}
                                {{ form_widget(cours.parcours, {'attr': {'class': 'form-control'}}) }}
                            </div>
                        </div>
                        <br>
                        <div class="text-center">
                            <input class="btn btn-flat btn-warning" type="submit" value="Enregistrer" />
                        </div>
                        {{ form_end(cours) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
